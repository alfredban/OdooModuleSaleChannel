<?xml version="1.0" encoding="UTF-8"?>
<odoo>

        <record id="action_sale_channel" model="ir.actions.act_window">
            <field name="name">Sales channels</field>
            <field name="res_model">sale.channel</field> <!-- modelo al que responde -->
            <field name="view_mode">list,form</field> <!-- accion a realizar -->
        </record>
    
         <menuitem id ='menu_sale_channel_root'
              name ="Sales channels"
              parent ="sale.menu_sale_config"
              action="action_sale_channel"
              sequence="10"/>

        <record id="action_credit_group_channel" model="ir.actions.act_window">
            <field name="name">Credit groups</field>
            <field name="res_model">credit.group</field> <!-- modelo al que responde -->
            <field name="view_mode">list,form</field> <!-- accion a realizar -->
        </record>
    
         <menuitem id ='menu_credit_group_root'
              name ="Credit groups"
              parent ="sale.menu_sale_config"
              action="action_credit_group_channel"
              sequence="10"/>

</odoo>
